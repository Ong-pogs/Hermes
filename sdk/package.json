{
  "name": "@smartcontractapi/solana",
  "version": "0.1.0",
  "description": "Solana smart contract primitives as simple function calls — escrow, token ops, and more without writing Rust",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "jest"
  },
  "keywords": ["solana", "escrow", "smart-contract", "sdk", "web3", "defi"],
  "license": "MIT",
  "dependencies": {
    "@coral-xyz/anchor": "^0.31.1",
    "@solana/web3.js": "^1.98.0",
    "@solana/spl-token": "^0.4.12"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "@types/node": "^22.0.0"
  },
  "peerDependencies": {
    "@solana/web3.js": "^1.90.0"
  }
}
